package sockets;


import javax.websocket.*;
import java.io.IOException;
import java.net.URI;

//@ClientEndpoint
public class ClientSocket {

  /*  @OnMessage
    public void message(String msg, Session session)
    {
        System.out.println(" This is Client " + msg);
    }

    public static void main(String[] args) throws IOException, DeploymentException, InterruptedException {

            WebSocketContainer container= ContainerProvider.getWebSocketContainer();
            Session session=container.connectToServer(ClientSocket.class, URI.create("ws://localhost:8080/EE3_war_exploded/MySocketServer"));
            session.getBasicRemote().sendText("SAI RAM");


    }*/
}
